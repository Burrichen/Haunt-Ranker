/**
 * The application's version, taken from package.json at build time by Vite
 * (see `define` in vite.config.ts) rather than from the Tauri API, so it's
 * available in tests and in a plain browser dev server too.
 */
export const APP_VERSION: string = __APP_VERSION__;
